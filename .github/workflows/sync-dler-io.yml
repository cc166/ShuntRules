name: Sync dler-io Rules

on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * *'  # 每天 UTC 03:00

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Sync dler-io/Rules
        run: python3 sync_dler_io.py

      - name: Commit and Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add surge/ _sync_report_dler_io.json
          if git diff --cached --quiet; then
            echo "No changes from dler-io/Rules"
            exit 0
          fi
          git commit -m "sync: update dler-io/Rules ($(date -u '+%Y-%m-%d %H:%M UTC'))"
          git push
